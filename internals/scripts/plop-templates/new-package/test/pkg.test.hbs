const {{camelCase name}} = require("..");
describe("{{name}} cjs", () => {
  test("should be defined", () => {
    expect({{camelCase name}}).toBeDefined();
    // expect({{camelCasename}}.default).toBeInstanceOf(Function);
  }); 
});